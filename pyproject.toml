[project]
name = "optimize-3d"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gurobipy>=13.0.1",
    "matplotlib>=3.0",
    "numpy>=1.0",
]

[project.scripts]
optimize-3d = "optimize_3d.__main__:main"

[tool.setuptools.packages.find]
include = ["optimize_3d*"]
